import os

# The following is commented out for purposes of not accidentally running it again
# Also, it will allow the re-use of it in the future with little modification

'''
parent_dir = 'H:/Python Projects/30DaysOfPython/'
dir_prefix = 'day_'

for i in range(1, 31):
    path = os.path.join(parent_dir, dir_prefix+str(i))
    os.mkdir(path)
'''
